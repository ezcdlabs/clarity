# Build output
/git-clarity
*.exe
*.exe~
*.dll
*.so
*.dylib
dist/

# Test binaries
*.test
*.out
coverage.*
*.coverprofile
profile.cov

# Go workspace (local only)
go.work
go.work.sum

# env file
.env

# OS
.DS_Store
Thumbs.db

# Claude Code
.claude/

# Local-only demo artifacts (the .gif IS committed because the README
# renders it inline; the .mp4 is regenerable for Discord/Slack sharing,
# and per-frame PNGs are explicitly opt-in via FRAMES=1).
/assets/*.mp4
/assets/*.frames/